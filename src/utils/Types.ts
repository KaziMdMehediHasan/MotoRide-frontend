// login data type
export type TLoginData = {
    email: string,
    password: string
}

// account register data type

export type TRegisterData = {
    name: string,
    email: string,
    password: string,
    phone: string
}